const config = {
    db : {
        host:"localhost",
        user:"root",
        password:"",
        database:"lootee_db",
        waitForConnections: true,
        connectionLimit: 10,
        queueLimit: 0
    }
};

module.exports = config;