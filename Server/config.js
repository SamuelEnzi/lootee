const config = {
    db : {
        host:"**",
        user:"**",
        password:"**",
        database:"**",
        waitForConnections: true,
        connectionLimit: 10,
        queueLimit: 0
    }
};

module.exports = config;